if (Product.GetVar("Virus", true))
    {
    var extra = Product.Userbase * 0.05 * 1000;
    Product.DevCompany.MakeTransaction(extra, Sales, Malware_Payment);
    Product.AddToCashflow(0, 0, 0, extra, 0, Time);
     }
